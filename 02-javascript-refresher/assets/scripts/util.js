export let apiKey = "adfs56789";
export let abc = "abc";
// Could also just export a default value such as:
// export default "adfs56789";
// This means that the above string is the default value that is exported by the file.
// We can only have one default export or else we get an error